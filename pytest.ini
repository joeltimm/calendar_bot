# ~/calendar_bot/pytest.ini
[pytest]
# Put the repo root on sys.path so tests can `import app` / `import utils`.
pythonpath = .

markers =
    e2e: marks tests as end-to-end, requires live environment

# Add this line to explicitly tell pytest-dotenv which file to load for tests
env_files = .env.test
